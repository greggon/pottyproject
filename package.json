{
  "name": "noderest",
  "description": "Rest API with Node",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js"
  },
  "dependencies": {
    "express": "3.x"
  },
  "devDependencies": {
    "nodemon": "^1.11.0"
  }
}
